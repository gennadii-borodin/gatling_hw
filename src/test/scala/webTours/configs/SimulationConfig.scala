package webTours.configs

import scala.concurrent.duration._

object SimulationConfig {
    val intensity = 8
    val stagesCount = 8
    val stageDuration = 3.minute
    val rampDuration = 20.seconds
    val stabilityDuration = 1.hour
    val stabilibyIntensity = 5
}
